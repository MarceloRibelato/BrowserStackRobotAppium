name: Execute Mobile Test on BrowserStack
on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  run_tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Set up Python 3.10
        uses: actions/setup-python@v3
        with:
          python-version: "3.10"
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

#      - name: Upload do APK
#        run: |
#          curl -u "marceloribelato_ksi0ah:Eej2sRzekpDZaBDLJGKr" \
#          -X POST "https://api-cloud.browserstack.com/app-automate/espresso/v2/module-app" \
#          -F file=@app/Calculadora.apk

      - name: Executar teste no BrowserStack
        run: |
          curl -u "marceloribelato_ksi0ah:Eej2sRzekpDZaBDLJGKr" \
          -X POST "https://api-cloud.browserstack.com/app-automate/espresso/v2/module-build" \
          -d '{"testSuite": "bs://0ad8c4aa17fcbb397d84ff77c02dee10a938edbb", "devices": ["Samsung Galaxy S23 Ultra","Google Pixel 8 Pro"]}' \
          -H "Content-Type: application/json"
